import React from 'react';
import PropTypes from 'prop-types';

/**
 * Reset / Save header actions in the top right
 */
const ListNamer = props => {

	return (
		<>
			List Namer
		</>
	);
};

/**
 * Valid props
 */
ListNamer.propTypes = {};

export default ListNamer;
